# Copy to .env and fill in values. Used by docker-compose for the Postgres service.
# Required for: docker compose up postgres

POSTGRES_USER=aiagent
POSTGRES_PASSWORD=localpostgres
POSTGRES_DB=aiagent
